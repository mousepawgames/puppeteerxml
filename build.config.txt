# Specify the paths to the dependencies here.

# PawLIB and CPGF are both at this location.
set(PAWLIB_DIR
	../../pawlib-git/pawlib
)

# Xerces source lives in the repository.
# You may choose to specify another path to it if you wish.
set(XERCES_DIR
    ../xerces/src
)
